#include <stdio.h>

int main()
{
    float len, bre, ar;
    printf("enter the length: ");
    scanf("%f",&len);
    printf("enter the breadth: ");
    scanf("%f",&bre);
    ar=(len*bre);
    printf("The area of the rectangle is %f",ar);
    return 0;
}
